import type { Meta, StoryObj } from "@storybook/react";

import { SignIn } from ".";

const meta: Meta = {
  title: "Screens/SignIn",
  component: SignIn,
  args: {},
  argTypes: {},
};

export default meta;

export const Default: StoryObj = {};
